__include__: [
  '../dataset/hw2_detection.yml',
  '../runtime.yml',
  './include/dataloader.yml',
  './include/optimizer.yml',
  './include/rtdetr_r50vd.yml',
]

output_dir: ./output/0417_rtdetr_r50vd_6x_hw2

# 找到或是新增 lr_scheduler 部分
lr_scheduler:
  type: CosineAnnealingLR
  T_max: 72    
  eta_min: 0.000001 # 學習率下降的最低點